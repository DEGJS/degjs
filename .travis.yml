language: node_js
node_js:
- stable
cache:
  directories:
  - node_modules
before_deploy:
- ACCESS_TOKEN=$ACCESS_TOKEN npm run-script update-demo
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $ACCESS_TOKEN
  keep-history: true
  target-branch: master
